#include "values.h" // when uses "" in include directive - preprocessor look for this file in the same directory 
#include <stdio.h> // when uses <> in include directive - preprocessor look for this file in installation path

int main(void)
{
    int x = SIZE * COUNT;
}